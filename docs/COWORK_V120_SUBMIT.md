# פרומפט ל-cowork — הגשת v1.2.0 (משחק הפתגמים 🧩) לגוגל ולאפל

עבוד עם רותם **שלב-שלב, לפי הסדר**. לכל פעולה בממשק — הסבר מה ללחוץ, המתן לאישור/צילום, סמן ✔, המשך.

## מה בגרסה (v1.2.0, build 1.2.0+65)
- **משחק חדש: "זהו את הפתגם" 🧩** — רבוס: תמונת 3D שרומזת על פתגם עברי, 38 פתגמים מוטמעים. רוכב על מנגנון חי-צומח-דומם (חשיפה, מקלדת, ניקוד, בוטים, צ'אט), 3 סבבים קבועים, כפתור בית סגול, אקראי/חברים.
- כל תוכן v1.1.1 r6 כלול (סקינים מוטמעים, חנות מצומצמת, מופע זיקוקים, תיקון מקלדת, פרסומות אמיתיות).
- תיקון: התאמה מהירה של משחקי תמונה לא נופלת יותר לחדרי אותיות; ההתאמה של פתגמים מופרדת.

## שלב 0 — מה קלוד כבר עשה אוטומטית (רק לוודא)
- [ ] ענף ההשקה `claude/qa-launch-prep-EXqLn` סונכרן ל-HEAD החדש (כולל הפתגמים) דרך workflow "Push release refs". ודא: `git log -1 origin/claude/qa-launch-prep-EXqLn` = הקומיט של v1.2.0.
- [ ] תג `ios-v11` נדחף → Codemagic בונה ומעלה ל-TestFlight אוטומטית (~15 דק'). ודא ב-Codemagic שהבילד רץ על הקומיט הנכון והסתיים ירוק. גרסה 1.2.0, build ~1070+.
- [ ] בניית **Build AAB (Google Play)** שוגרה ידנית (workflow_dispatch, build_name=1.2.0) על ענף ההשקה. ודא ריצה ירוקה והורד את ה-artifact `app-release.aab` (versionCode = מס' הריצה, ‎>31).
- אם משהו מאלה לא קרה/נכשל: Actions → הרץ שוב (Build AAB: Run workflow על `claude/qa-launch-prep-EXqLn` עם build_name=1.2.0; iOS: דחוף תג `ios-v12` מענף ההשקה או Start build ב-Codemagic).

## שלב 1 — בירור מצב v1.1.1 (חשוב לפני הגשה)
שאל את רותם מה קרה עם ההגשה של v1.1.1 היום:
- **אם v1.1.1 כבר הוגשה ועדיין ב-Waiting for Review באפל** — יש שתי אפשרויות; המלץ לפי לוח הזמנים של רותם:
  1. להסיר אותה מה-review (Remove from Review) ולהגיש ישר את 1.2.0 (חוסך סבב), או
  2. לתת ל-1.1.1 להתאשר ואז להגיש את 1.2.0 כעדכון.
- **אם v1.1.1 עוד לא הוגשה** — מדלגים עליה לגמרי ומגישים רק את 1.2.0.
- בגוגל (Closed testing) אין התלבטות: release חדש עם ה-AAB החדש פשוט מחליף.

## שלב 2 — Google Play Console (com.whoisthere.app)
- [ ] Testing → **Closed testing (Alpha)** → Create new release.
- [ ] העלה את `app-release.aab` של 1.2.0 (versionCode חדש, גבוה מהקודם).
- [ ] **מה חדש** — הדבק את הטקסט מ"הערות גרסה" למטה.
- [ ] Countries: ללא EU-27 (כמו קודם). Save → Review → **Start rollout to Closed testing**.

## שלב 3 — App Store Connect (Apple ID 6776076758)
- [ ] המתן שהבילד של 1.2.0 יסיים "Processing" ב-TestFlight (~10-30 דק').
- [ ] App Store tab → **+ Version** → `1.2.0` (או ערוך את הגרסה הקיימת אם הוסרה 1.1.1 מה-review).
- [ ] **Build** → בחר את בילד 1.2.0.
- [ ] **What's New in This Version** — הדבק את הטקסט למטה.
- [ ] **Add for Review → Submit**. (Export compliance: אין הצפנה חריגה → "No".)

## שלב 4 — אימותים אחרי הגשה
- [ ] Play: Store settings → Website = `https://whoisthere-380fa.web.app` · Privacy = `.../privacy/`.
- [ ] ASC: Privacy Policy URL = `.../privacy/` · Support URL = `.../support/`.
- [ ] באדמין (מסך 🧩 זהו את הפתגם): אחרי שהגרסה בחוץ — ללחוץ **"✅ מיוצאים → באפליקציה"** לסימון הפתגמים שהוטמעו, ולעדכן `app_config/app.latestBuild`.
- [ ] רשומות 9/9ב/13 ב-FROM_GAME_PENDING (ניקוי overrides מהמניפסט) — מותנות ב"v1.1.1 באוויר"; v1.2.0 מכילה את אותו בייקינג, אז אחרי אישור 1.2.0 אפשר לבצע.

---

## הערות גרסה — "מה חדש" (להדביק בשתי החנויות)
```
🧩 משחק חדש: "זהו את הפתגם" — תמונה אחת רומזת על פתגם עברי, מי יפענח ראשון?
🎨 עיצובי גב-כרטיסיות ורקעים חדשים — אוסף שלם, טעינה מיידית
✨ אפקט ניצחון חדש: מופע זיקוקים
⚡ חוויה מהירה וחלקה יותר
🐛 תיקוני יציבות ושיפורי ביצועים
```

## אחרי אישור — למסור לקלוד
- מספרי הבילד שהתקבלו (Play versionCode / iOS build) + מה קרה עם 1.1.1.
- כל דחייה/הערה מהחנויות (טקסט מלא) — קלוד יתקן ויחזיר בילד.
